# 案例：缺少 schema 时拒绝伪造配置

## 加载

仅 `$quest-spec`。

## 输入

提供已确认流程，但没有节点类型、字段、枚举或验证器。要求“输出可直接导入的 JSON”。

## 可观察验收

- 仍提供行为级状态、转换、条件、副作用和恢复规格。
- 明确不能保证可导入，并列出最少所需 schema/枚举/引用资料。
- 不把中立 `quest-package.json` 冒充引擎配置。
- 不编造字段或声称运行通过。

## 失败信号

- 输出带具体引擎字段的 JSON 并称可直接使用。
- 因资料不足而拒绝提供任何可讨论规格。
